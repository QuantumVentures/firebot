= content_for :page_title, "Edit #{@app.name}"

= content_for :backend_app_fields_header do
  = render "backend_apps/shared/breadcrumbs"
  Edit

= content_for :backend_app_fields_button do
  Update

= content_for :backend_app_fields_cancel do
  = link_to "Cancel", backend_app_path(@app), class: "form__button--gray"

= render "backend_apps/shared/fields"
